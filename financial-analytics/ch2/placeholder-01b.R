# Define PV function: calc_pv
calc_pv <- function(fv, r, n){
    pv <- ___
    pv
}

# Use PV function for a single input
calc_pv(100, 0.08, ___)

# Use PV function for range of inputs
n_range <- ___
pv_range <- calc_pv(100, 0.08, ___)
pv_range
